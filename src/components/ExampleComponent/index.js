import React from 'react';
import { ExampleElement } from './style';

export default class HomePage extends React.Component {
  render() {
    return (
      <ExampleElement>
        example component
      </ExampleElement>
    );
  }
}